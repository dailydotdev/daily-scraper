apiVersion: v1
kind: Secret
metadata:
  name: {{ template "daily-scraper.fullname" . }}-files
  labels:
    app: {{ template "daily-scraper.name" . }}
    chart: {{ template "daily-scraper.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  serviceAccount.json: |-
    {{ .Values.appConfig.serviceAccount }}

